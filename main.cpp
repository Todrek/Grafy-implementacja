#include <iostream>
using namespace std;

#define LL long long
#define SIZE(x) (int) x.size()
#define PB(x) push_back(x)

#include "Grapth.h"

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    return 0;
}
